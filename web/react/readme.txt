Step 1: 
    Install nodejs (First time only)

Step 2: 
    Put this cmd into powershell (First time only)
        - Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

Step 3: 
    Install react app with Vite plugin
    - npm create vite@latest

Step 4:
    Goto exact folder and run this command 
    - npm run dev

    Incase you don't have node_modules folder then run this cmd 
    first
    - npm i / npm install

______________________________________

Topics
    Pre-req
        - html, css, js

Components
hooks
    js => const list = []
    react => const [ list, setList ] = useState([])
Props
Childrens
Layouts
State management

pkgs


